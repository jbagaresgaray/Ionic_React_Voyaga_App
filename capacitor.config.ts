import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_react_voyaga',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
